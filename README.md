We are having multiple files(csv and json) in GCS from source. we have to clean the data using pyspark scripts and convert them to parquet files, write them to GCS. once we got curate files in GCS we will be taking them and inserting into bigquery. we will be using airflow for orceshration.


